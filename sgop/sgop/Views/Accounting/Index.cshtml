
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div class="row my-3">
    <div class="col-6">
        @using (Html.BeginForm("controlPagos", "Accounting", FormMethod.Post, new { @id = "llamarPagos" }))
        {
            <input type="hidden" name="idLicitacion" value="100006" />
        }
        <button type="button" class="btn btn-primary btn-sm w-100" onclick="llamarPagos.submit();">Control de pagos</button>
    </div>
</div>
<div class="row my-3">
    <div class="col-6">
        @using (Html.BeginForm("controlCobros", "Accounting", FormMethod.Post, new { @id = "llamarCobros" }))
        {
            <input type="hidden" name="idLicitacion" value="100006" />
        }
        <button type="button" class="btn btn-primary btn-sm w-100" onclick="llamarCobros.submit();">Control de pagos</button>
    </div>
</div>

